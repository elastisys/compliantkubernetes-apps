apiVersion: templates.gatekeeper.sh/v1
kind: ConstraintTemplate
metadata:
  name: k8snoclusterdeletion
spec:
  crd:
    spec:
      names:
        kind: K8sNoClusterDeletion
  targets:
    - target: admission.k8s.gatekeeper.sh
      rego: |
{{ .Files.Get "policies/no-cluster-deletion.rego"  | indent 8 }}
