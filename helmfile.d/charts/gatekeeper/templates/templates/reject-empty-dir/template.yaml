apiVersion: templates.gatekeeper.sh/v1
kind: ConstraintTemplate
metadata:
  name: k8srejectemptydir
spec:
  crd:
    spec:
      names:
        kind: K8sRejectEmptyDir
      validation:
        # Schema for the `parameters` field
        openAPIV3Schema:
            type: object
            properties:
              annotation:
                description: This annotation allows to suppress the warning of the constraint if the .
                type: string
  targets:
    - target: admission.k8s.gatekeeper.sh
      rego: |
{{ .Files.Get "policies/reject-empty-dir.rego"  | indent 8 }}
