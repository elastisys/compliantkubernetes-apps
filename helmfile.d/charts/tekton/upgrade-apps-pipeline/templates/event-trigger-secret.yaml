{{- if .Values.tekton.listener.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "upgrade-apps-pipeline.fullname" . }}-github-secret
type: Opaque
stringData:
  secret: {{ .Values.tekton.eventTriggerSecret }}
{{- end }}
