apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-label-enforcer.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "grafana-label-enforcer.labels" $ | nindent 4 }}
spec:
  ports:
  - name: promql
    port: 9090
    targetPort: promql
  selector:
    app: {{ include "grafana-label-enforcer.fullname" . }}
