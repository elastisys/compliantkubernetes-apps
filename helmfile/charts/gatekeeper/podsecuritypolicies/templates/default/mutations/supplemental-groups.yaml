apiVersion: mutations.gatekeeper.sh/v1
kind: ModifySet
metadata:
  name: mutation-run-as-user-supplemental-groups
spec:
  applyTo:
    - groups: [""]
      kinds: ["Pod"]
      versions: ["v1"]
  match:
    scope: Namespaced
    kinds:
      - apiGroups: ["*"]
        kinds: ["Pod"]
    namespaces:
    {{- range $namespace, $services := .Values.constraints }}
      - {{ $namespace }}
    {{- end}}
    {{- if .Values.mutationExceptions }}
    labelSelector:
      matchExpressions:
        {{- range $value := .Values.mutationExceptions }}
        {{- range $labelkey, $labelvalue := $value }}
        - key: {{ $labelkey }}
          operator: NotIn
          values:
            - {{ $labelvalue | quote }}
        {{- end }}
        {{- end }}
    {{- end }}
  location: "spec.securityContext.supplementalGroups"
  parameters:
    values:
      fromList:
        - 65535
    pathTests:
      - subPath: "spec.securityContext.supplementalGroups"
        condition: MustNotExist
