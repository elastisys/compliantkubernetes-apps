apiVersion: v1
kind: Secret
metadata:
  name: kured-secret
type: Opaque
data:
  {{ if .Values.slack.enabled }}
  notifyUrl: {{ (printf "slack://%s@%s" .Values.slack.botToken .Values.slack.channel ) | b64enc }}
  {{ end }}
