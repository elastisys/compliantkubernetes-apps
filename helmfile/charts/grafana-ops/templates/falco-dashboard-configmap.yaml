{{- if .Values.dashboards.falco.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: falcodashboard
  namespace: monitoring
  labels:
    {{ .Values.labelKey }}: 'falco'
data:
  falco-dashboard.json: |-
    {{- .Files.Get "dashboards/falco-dashboard.json" | nindent 4 }}
{{- end }}
