apiVersion: kubernetes.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: kubernetes-wc
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: wc-kubeconfig
      key: value
